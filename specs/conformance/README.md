# Conformance fixtures

This directory is reserved for versioned, synthetic conformance vectors.
Fixtures must not require live feeds, private identities, credentials, or
network access. The bootstrap slice only reserves the location; semantic
validators will be added with the composition milestone.
