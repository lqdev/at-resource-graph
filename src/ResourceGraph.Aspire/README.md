# Aspire boundary

This project intentionally contains contracts only. It does not install an
Aspire workload, deploy a PDS, start a relay, or add firehose services. A
future local topology can implement `IResourceGraphTopology` after the
protocol-independent reader and AppView contracts stabilize.
